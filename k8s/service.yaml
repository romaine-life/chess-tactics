apiVersion: v1
kind: Service
metadata:
  name: chimera-board
  namespace: chimera-board
  labels:
    app: chimera-board
spec:
  type: ClusterIP
  selector:
    app: chimera-board
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
