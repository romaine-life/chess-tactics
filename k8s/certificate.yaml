apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: chess-tactics-tls
  namespace: chess-tactics
spec:
  secretName: chess-tactics-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - chess.romaine.life
