apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: chimera-board-tls
  namespace: chimera-board
spec:
  secretName: chimera-board-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - chimera-board.romaine.life
