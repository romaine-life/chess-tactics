# Codex independent v3 archive note

This historical attempt separated atom-only and rail-only source lanes. Its
exact sheets are archived in live storage under their original repository paths;
they are not a committed or installed kit. Future extraction starts from an
authenticated archived source and uploads resulting candidates directly.

Do not assemble from source art where an atom already includes rail arms.
