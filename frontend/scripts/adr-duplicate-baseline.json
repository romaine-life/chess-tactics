{
  "comment": "Empty on purpose. Every ADR number now identifies exactly one decision; check-adr-numbers.mjs fails the moment that stops being true, so a new collision is caught on the branch that creates it rather than becoming debt. Keep this file: it is the seam that let the original 23 collisions be drained one at a time instead of in one unreviewable sweep.",
  "duplicates": []
}
