{
  "name": "chess-tactics-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node --check src/app.js && node scripts/check-no-chrome-bitmaps.mjs"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "vite": "^8.0.16"
  }
}
