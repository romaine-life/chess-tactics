
# Local headless-screenshot output (frontend/scripts/shot.mjs)
tmp-shots/

# Local generation scratch: badge review output, codex method-gate evidence, desc files
tmp-badges/
tmp-forge-evidence/
tmp-forge-studio/
tmp/

# Local append-only audit trail of nine-slice bakes (scripts/nine-slice-kit.mjs)
config/nine-slice/save-log.jsonl
scripts/__pycache__/

# Headless trainer engine bundle (built by `npm run build:trainer` in the Docker
# frontend stage, copied into the runtime image; never committed).
trainer-bundle/
